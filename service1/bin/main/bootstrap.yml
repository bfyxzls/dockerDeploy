auth.name: test
server.port: 7788
spring:
  application:
    name: service1
  profiles:
      active: development
---
spring:
  profiles: development
  cloud:
    config:
       uri: http://${MY_IP:localhost}:6200

---
spring:
  profiles: svt
  cloud:
    config:
       uri: http://configserver:6200