auth.name: test
spring:
  profiles:
      active: development
  application:
      name: service1
---
spring:
  profiles: development
  cloud:
    config:
       uri: http://localhost:6200

---
spring:
  profiles: svt
  cloud:
    config:
       uri: http://configserver:6200