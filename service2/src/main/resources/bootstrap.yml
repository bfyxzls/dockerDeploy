spring:
  profiles:
      active: development
  application:
      name: service2
---
spring:
  profiles: development
  cloud:
    config:
       uri: http://${MY_IP:localhost}:6200

---
spring:
  profiles: svt
  cloud:
    config:
       uri: http://configserver:6200